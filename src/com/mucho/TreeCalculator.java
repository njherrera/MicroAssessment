package com.mucho;

public class TreeCalculator {

    // since the tree was constructed from postfix notation, we'll be evaluating it using an inorder traversal
    public double calculateTreeValue (Node root){
        if (root != null){
            
        }
    }
}
